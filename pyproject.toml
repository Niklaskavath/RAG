[project]
name = "rag"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "ollama>=0.6.2",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
    "mypy>=2.3.0",
    "pip>=26.2.1",
    "pytest>=9.1.1",
    "pytest-mock>=3.15.1",
    "ruff>=0.16.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
